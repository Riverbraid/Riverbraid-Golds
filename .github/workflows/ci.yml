name: CI
on: [push, pull_request]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify Stationary State
        run: |
          if [ -f "verify.mjs" ]; then node verify.mjs; else echo "No local verify script found."; fi
