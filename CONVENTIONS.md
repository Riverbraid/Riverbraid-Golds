# 📜 Cluster Conventions
* **protocol.steps**: Sequential manifest of deterministic state transitions.
* **verify.mjs**: Local petal auditor (exit 0 if stationary).
* **types/**: TypeScript/JSDoc definitions for signals.
