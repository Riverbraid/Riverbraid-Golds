{
  "name": "@riverbraid/golds",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.11.0",
    "npm": "10.2.4"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present",
    "build": "node scripts/verify-closure.mjs && node scripts/entropy-scan.mjs && node scripts/run-vectors.mjs",
    "golds": "npm run build"
  }
}
