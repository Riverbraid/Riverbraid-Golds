[package]
name = "riverbraid"
version = "1.1.0"
edition = "2021"

[dependencies]
sha2 = "0.10"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
flate2 = "1.0"
hex = "0.4"

[profile.release]
panic = "abort"
lto = true
